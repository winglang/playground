{
  "name": "@wing-playground/monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "update": "turbo run update",
    "tsc": "turbo run tsc"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "turbo": "^1.10.3"
  }
}
