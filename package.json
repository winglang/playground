{
  "name": "@wing-playground/monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "update": "turbo run update",
    "learn": "turbo run dev --filter=@wing-playground/tour",
    "play": "turbo run dev --filter=@wing-playground/playground",
    "tsc": "turbo run tsc"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "turbo": "^1.10.3"
  }
}
