## Using a Counter inflight

1. The inflight API of cloud.Counter allows you to increment, decrement, reset or peek its value.
2. Notice the following code that uses `inc`, `dec`, `set`, and `peek` calls.
3. In the Wing Simulator (on the right) click on the "Example Code" resource.
4. Invoke the Function (using the right panel), Notice the logs on the bottom.
5. Click on the counter resource, and use the right panel to interact with it.
