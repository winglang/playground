## Using a bucket inflight

1. The inflight API of cloud.Bucket allows you to create,list and delete objects.
2. Notice the following code that uses `put`, `putJson`, `get`, `getJson` and `list` calls.
3. In the Wing Simulator (on the right), click on the "Example Code" resource.
4. Invoke the Function (using the right panel). Notice the logs at the bottom.
5. Click on the cloud.Bucket resource, and use the file browser (right panel) to see `file.json`.
