# Push a Message to the Queue Programmatically

Start by adding code to push the function's payload to the queue on line 7. (Hint: Look at how the payload is added to the log on line 6).

Afterwards, invoke the **cloud.Function** in the simulator. Take note that it's now connected to the queue.

Observe the changes to the **cloud.Queue** in the simulator.

In the next step we will consume these pushed messages
