# Inflight - Compilation artifacts

You can see the JavaScript code that is generated when compiling our inflight code by going to the ***AWS/Terraform compilation target window*** and looking in the ***Assets*** section (below the Resources one).

Click the "inflight1.js" file. The "handle" function is the one that will be executed when the cloud.Function is invoked.

Now look at the Resources section above the Assets. 

The cloud.Function that we defined in the preflight code is there and it is implemented by a Lambda (see the "Lambda" it contains). 

The Lambda needs to be able to run the JavaScript code when it is invoked. So the code is uploaded to an S3 bucket that the Lambda can access.

The S3 bucket is the S3 under the Default resource. The file with the code that is uploaded to it is the S3 Object under the cloud.Function resource.

Click ***Next*** to learn about how to reference preflight data from inflight code.