bring cloud;

let bucket = new cloud.Bucket();
// type `bucket.` to see the bucket's preflight interface in the autocomplete suggestions


let hello_world = inflight () => {
  // type `bucket.` to see the bucket's inflight interface in the autocomplete suggestions

};

new cloud.Function(hello_world);