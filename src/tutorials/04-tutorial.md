# Push a message to the Queue programmatically

1. **Editor:** In the `cloud.Function` code, push a message to the queue.
2. **Simulator:** Invoke the Function.
3. **Simulator:** Explore what happens in the queue.
4. **Editor:** Type `q.` outside the Function. 
5. **Editor:** See how the completion options are different there 💡
