# Push a message to the Queue programmatically

1. **Editor:** In line #7, add code to push a message to the queue
2. **Editor:** Pass the function's payload to the queue (like with the log - line #6).
3. **Simulator:** Invoke the Function (notice it's connected to the queue now).
4. **Simulator:** Explore what happens in the queue.
   
🏆 **Bonus:** Look at the code comment in line #10.