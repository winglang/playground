name: Release Wing Playground

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  release:
    name: Release Wing Playground
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: main
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
      - name: Install dependencies
        run: npm install
      - name: Update assets
        run: npx tsx scripts/updatePlayground.ts
        env:
          GITHUB_TOKEN: ${{ secrets.PROJEN_GITHUB_TOKEN }}
      - name: Build playground
        run: npm run build
      - name: "Publish Wing Playground"
        uses: amondnet/vercel-action@v25.1.0
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: .
          github-comment: false
          vercel-args: "--prod "
      - name: Commit files
        run: |
          git config user.name "monabot"
          git config user.email "monabot@monada.co"
          git commit -a -m "Auto update assets [skip ci]"
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.PROJEN_GITHUB_TOKEN }}
          branch: ${{ github.ref }}